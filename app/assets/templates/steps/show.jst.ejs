<a href="#/chains/<%=step.escape("chain_id")%>" data-id="<%=step.id%>" class="step">
<% if(step.is_image()) { %>
    <!-- make link here instead maybe maybe? -->
  <div class="step-url" data-chain_id="<%=step.escape("chain_id")%>">
    <%=step.escape("url")%>
    <!-- should parse image instead of displaying URL -->
  </div>
<% } else { %>
  <div class="step-desc" data-chain_id="<%=step.escape("chain_id")%>">
    <%=step.escape("description")%>
  </div>
<% }; %>
</a>

<!-- Try to fix later, currently doubley nested div -->

<!-- href="#/chains/<%=step.escape("chain_id")%>" -->